on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
  
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.6
      with:
        server: kmfoysal06.com
        server-dir: /blog.kmfoysal06.com/wp-content/themes/blockblog
        local-dir: ./
        username: kmfoysa1
        password: ${{ secrets.password }}
